<!DOCTYPE html>
<html>
  <head><title>Photo privée</title></head>
  <body>
    <% if (unlocked) { %>
      <h2>✅ Merci pour votre achat</h2>
      <img src="/image-clear.jpg" width="300" />
    <% } else { %>
      <h2>🔒 Photo floutée</h2>
      <img src="/image-blur.jpg" width="300" />
      <form action="/create-checkout-session" method="POST">
        <button type="submit">Débloquer cette image</button>
      </form>
    <% } %>
  </body>
</html>
